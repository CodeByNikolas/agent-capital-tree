{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "kanoki": {
      "type": "stdio",
      "command": "node",
      "args": [
        "./capital.mjs", "stdio", "--enable-sepolia-writes"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
